# Task 2: Make the app successfully running for the first time
Task: Fix the Pebble app.
Currently, the app builds correctly but running it in emulator and selecting the app within the QEMU emulator, does nothing and shows the error logs only. The app doesn't open, perhaps it crashes.
Possibly, rewrite it from scratch with ANYTHING that runs but it simple. And then improve. You can look for the example apps to start with, and modify, and build on top of that.
Use Modern Javascript approach, recommended for Pebble apps. If Rocky.js is obsolete, Get RID OF IT!
I think now we should use Alloy framework instead.
Fix it.

The app builds and installs successfully, but crashes or doesn't display when launched in emulator.

**Use the emulator control workflow to:**
1. Capture screenshots after app launch
2. Check logs for error messages  
3. Iterate on fixes and verify visually 
